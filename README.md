Forward UDP ports 500 and 4500.  Give the designated host an alias of `vpn`.

So far the server end seems to run, but an android client just says "Connecting...", and nothing shows up in the server log (syslog) upon connection attempt.
